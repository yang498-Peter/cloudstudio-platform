export const APP_PAGES = Object.freeze({
  INDEX: 'index',
  VIEWER: 'viewer',
});

export const TOAST_DEFAULT_MS = 4000;

export const STATUS_KEYS = Object.freeze({
  INDEX_WAITING_UPLOAD: 'waitingUpload',
});
