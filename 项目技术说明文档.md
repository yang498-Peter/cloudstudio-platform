# CloudStudio 项目技术说明文档（面向交接/对外介绍）

> 文档目的：让第一次接触该项目的人，快速理解它“是什么、怎么做的、当前做到哪里、如何部署与维护”。
> 依据：已阅读本机 `potree-local/cloudstudio-server` 目录内核心文件（`server.js`、`index.html`、`viewer.html`、`setup.sh`、`DEPLOY*.md`、`web-uploader/README.md`）。

---

## 1. 项目是什么

**CloudStudio** 是一个云端化的点云处理与浏览平台，面向激光点云（LAS/LAZ）和扫描仪项目数据（点云 + 照片 + 轨迹 + 坐标系）。

它提供两层入口：

1. **主页（`index.html`）**：项目介绍 + 上传入口 + 已有点云列表
2. **专业查看器（`viewer.html`）**：基于 Potree 的 3D 点云浏览、测量、分析、导出

核心目标是把“本地文件夹工作流”迁移到云端，实现：
- 上传后自动转换为 Potree 可视化格式
- 对扫描仪项目保留完整上下文（轨迹、照片、坐标等）
- 通过浏览器直接完成查看与分析

---

## 2. 技术栈与工程结构

## 2.1 主要技术栈

- 前端：`HTML + CSS + 原生 JavaScript`
- 3D 渲染：`Potree`（依赖 Three.js）
- 后端：`Node.js + Express + Multer`
- 点云转换：`PotreeConverter`（Linux 侧编译）
- Python 工具链：用于导出、DTM、等高线、地面分类等脚本
- 进程守护：`PM2`
- 反向代理：`Nginx`

## 2.2 目录结构（关键）

```text
cloudstudio-server/
├── setup.sh                  # 一键部署脚本（Ubuntu）
├── DEPLOY.md                 # 部署说明
├── DEPLOY_SOP.md             # 发布SOP（含热更新/全量策略）
├── potree/                   # Potree 前端库
├── PotreeConverter/          # 转换器源码与构建目录
└── web-uploader/
    ├── server.js             # 后端主服务（API + 转换调度）
    ├── index.html            # 主页/上传/项目列表
    ├── viewer.html           # 专业点云查看器
    ├── assets/               # CRS、静态资产
    ├── scripts/              # Python处理脚本
    ├── uploads/              # 上传临时文件
    ├── projects/             # 上传后的扫描项目解压目录
    ├── pointclouds/          # 转换后的点云输出
    ├── exports/              # 导出文件
    └── cache/                # 缓存与注册信息
```

---

## 3. 后端架构（`web-uploader/server.js`）

## 3.1 核心路径

- 服务端口：默认 `8090`
- 关键目录常量：
  - `UPLOADS_DIR`：接收上传原文件
  - `POINTCLOUDS_DIR`：Potree 转换产物
  - `PROJECTS_DIR`：扫描仪 ZIP 解压后目录（已纳入扫描根）
  - `EXPORTS_DIR`：导出结果

静态服务：
- `/potree`、`/pointclouds`、`/projects`、`/exports`、`/assets`

## 3.2 关键 API（已落地）

- 扫描项目管理
  - `GET /api/scan-projects`
  - `POST /api/scan-projects/register`
  - `GET /api/scan-roots`

- 点云与上传
  - `GET /api/clouds`（已增强：可返回 scanner 元信息）
  - `POST /api/upload`（单 LAS/LAZ 上传）
  - `POST /api/upload-project`（ZIP 项目上传）
  - `GET /api/cloud-source`（读取 `source.json` 并关联扫描项目）

- 处理与分析
  - `POST /api/generate-dtm`
  - `POST /api/generate-contours`
  - `POST /api/classify-ground`

- 健康检查
  - `GET /health`

## 3.3 扫描仪项目机制

系统有一个“扫描项目注册表”概念，依据 marker 文件和目录识别项目特征（轨迹/照片/地理信息等）。

**当前已实现的云端关键能力：**
1. 支持上传 ZIP 项目包
2. 服务端解压并保留层级结构
3. 自动选择合适 LAS 进行 Potree 转换
4. 在 `source.json` 写入：
   - `scannerProjectId`
   - `scannerProjectName`
   - `scanDataUrl`
5. 前端可据此自动恢复扫描项目上下文

---

## 4. 前端架构与交互

## 4.1 主页（`index.html`）

职责：
- 产品介绍（`/#advantages` 等）
- 上传入口（单文件与 ZIP 项目）
- 上传进度显示（进度、速度、阶段提示）
- 点云列表展示

最近增强点：
- 点云列表支持区分普通点云与扫描仪项目（徽标/标签）
- 展示 `features` 标签（如轨迹、相机、坐标）

## 4.2 查看器（`viewer.html`）

职责：
- Potree 点云可视化与工具集
- 通过 URL 参数 + `/api/cloud-source` 自动关联扫描项目
- 测量、剖面、地面分类、DTM/等高线流程

近期界面交互修复：
- 处理 Potree 原生汉堡按钮失效：
  - 劫持 `.potree_menu_toggle`
  - 改为折叠/展开自定义 `#left-panel`
  - 增加 `left-panel-collapsed` 状态样式

---

## 5. 最近功能演进（本版本重点）

以下是当前项目“云端可用性”相关的关键成果：

1. **ZIP 扫描项目上传工作流打通**
   - 单次请求上传
   - 服务端解压 + 自动转换
   - 可访问项目内照片/轨迹等内容

2. **扫描项目元信息贯通**
   - `source.json` 写入 scanner 关联字段
   - `/api/clouds` 返回 scanner 信息
   - 首页与 viewer 能正确识别并显示

3. **历史数据兼容修复**
   - 旧项目可通过补写 `source.json` 字段恢复关联

4. **ZIP 兼容性与稳健性修复**
   - 处理 Mac ZIP 中 `__MACOSX` 与 `._*` 垃圾文件
   - 优化 LAS 选择策略（避免 `uncolorized` 误选）
   - 增加路径存在性检查与日志，便于诊断

5. **发布流程治理（SOP）**
   - 新增“最小变更热更新优先”规则
   - 用户明确只传少量文件时，禁止擅自整包发布

---

## 6. 部署与运维模型

## 6.1 环境要求

- Ubuntu 22.04/24.04
- Node.js 18+
- Python3 + venv
- Nginx
- PM2
- 可编译 PotreeConverter 的构建环境

## 6.2 一键部署脚本（`setup.sh`）会完成

1. 安装系统依赖（含 `unzip`）
2. 安装或校验 Node.js
3. 编译 PotreeConverter
4. 配置 Python 虚拟环境与依赖
5. `npm install`
6. 配置 Nginx 与 PM2，启动服务

## 6.3 运行判断标准

`/health` 返回应包含：
- `ok: true`
- `converter: true`
- `exportPython: true`

---

## 7. 发布策略（当前团队约定）

来源：`DEPLOY_SOP.md`

- **默认走热更新（最小改动）**：只上传改动文件，重启 PM2
- **全量发布仅在必要时**：结构大改、用户明确要求、版本整体对齐
- 禁止在用户已明确“只传N个文件”时擅自整包覆盖

这条策略能显著减少发布时间与风险，并避免目录残留混淆（如 `cloudstudio.new`）。

---

## 8. 当前项目价值（对外话术可用）

CloudStudio 不是单纯“点云查看器”，而是一个面向生产场景的**云端点云处理工作台**：

- 既支持传统 LAS/LAZ 快速上线
- 也支持扫描仪项目的全量语义上下文
- 具备可持续扩展的处理能力（DTM、等高线、分类、导出）
- 已形成可复制的部署与发布规范

适合用于：
- 测绘/巡检/工程现场数据回传与远程分析
- 扫描项目的云端归档与协同查看
- 面向客户的在线成果展示与快速交付

---

## 9. 建议的后续优化（下一阶段）

1. **上传层**：分片上传、断点续传、失败重试
2. **任务层**：转换任务队列化（状态机 + 可观测）
3. **权限层**：多用户/项目权限隔离
4. **存储层**：对象存储（OSS）替代本地磁盘直存
5. **观测层**：统一日志与性能指标面板

---

## 10. 文档状态

- 文档文件：`cloudstudio-server/项目技术说明文档.md`
- 版本：`v1.0（基于当前本机代码与近期改动）`
- 用途：交接、汇报、对外介绍、二次开发入门
