# 2026-04-22

- 用户确认要正式重构软件的体积计算模块。
- 当前目标顺序：
  1. 先整理并沉淀一份正式的中文版 `Volume Rebuild Design Doc.md`
  2. 再基于该文档重做体积计算部分，包括逻辑、算法和 UI
  3. 后续持续在该文档中更新进度和计划
- 用户明确希望新的整体链路改为：
  - 先选区域
  - 构建可视化 mesh / 分析面
  - 让客户先看到用于计算的参考表面
  - 再基于该分析面计算体积
- 用户希望新方案兼容更多实际场景：
  - 拟合地面计算体积
  - 堆体体积
  - 提供草、树等凸起不计入体积的选项
- 用户允许使用子代理并行推进本次设计与重构工作。
- 当日已完成第一版落地：
  - 新建 `Volume Rebuild Design Doc.md`
  - 新增统一 volume job 后端链路
  - 前端 volume 面板改为围绕 analysis/base surface 结果展示
  - `viewer.html` 的主计算已改为服务端 volume job 驱动
- 当日烟测结果：
  - 本地 `volume job` 已在真实分类 LAS 上跑通
  - 已验证 `stockpile_boundary` 与 `plane_cut_fill + dtm/ground_only` 两种模式
  - 已修复 volume job 最初误走系统 Python、缺少 `numpy` 的问题，现改为优先使用项目 `.venv` Python
- 当日测试补强：
  - 新增 `web-uploader/tests/volume-job.integration.test.mjs`
  - `npm run test:volume` 已包含统一 volume job 集成测试并通过
- 当日附带修复：
  - 修复 viewer 首屏 DXF 初始化时序错误，避免 locale 应用时过早访问 `_dxfFiles`
- 当日能力补充：
  - volume 面板新增 point filter 选项
  - 后端统一 volume job 已接受 `exclude_vegetation_buildings`
  - volume 面板新增正式场景模式：`Stockpile` / `Plane Cut/Fill` / `Ground-Fitted Volume`
  - volume 面板开始显示场景解释文案与后端 diagnostic warnings
  - `Ground-Fitted Volume` 已能区分真正 ground fit 与 boundary fallback，并向 UI 透传说明
  - 新增 `ground_fit_volume` 集成测试，验证 class-2 地面支持路径
  - 基于真实 `/Users/yangqi/Downloads/BEL-JOEL/colorized.las` 验证无分类项目行为
  - `ground_fit_volume` 在无 class-2 时已改为 `ground_quantile_fallback`，不再直接误导为 ground fit
- BEL-JOEL 真实堆体区域已验证分辨率推荐：`1.0m` 过粗，推荐区间约 `0.3m ~ 0.58m`
- 已解决本地浏览器 smoke test 环境：
  - 新增 `web-uploader/tests/viewer-smoke.test.mjs`
  - 新增 `npm run test:viewer-smoke`
  - 通过本地 Playwright + `swiftshader` 解决 headless WebGL context 问题
  - 新增 `web-uploader/tests/viewer-volume-flow.test.mjs`
  - `npm run test:viewer-volume` 已验证真实 UI 路径：打开 BEL-JOEL 项目 -> 进入测量面板 -> 启动 volume 工具
- BEL-JOEL 的 `params.json` / `transforms.json` 缺失已确认属于可选扫描项目资源，不再作为 UI 流程测试失败条件
- 已完成一轮旧体积 dead code 清理：
  - 旧前端抽样限制函数不再残留引用
  - 旧 `surfaceGrid` 本地积分函数已移除
- 已新增体积 PDF 报告导出：
  - `volume job` 产出 `volume_report.pdf`
  - BEL-JOEL 真实项目报告已验证可下载、可读
- 报告版式已升级为更接近正式交付件，并做过真实视觉检查
- 新增 `web-uploader/tests/volume-report-visual.test.mjs`
- `npm run test:volume-report` 已验证 PDF 报告渲染与视觉尺寸检查
- 报告已继续打磨为更正式的交付版式，且 BEL-JOEL 真实报告已通过视觉复查
- 用户最新要求：堆体测量场景默认不依赖地面点分类，也不假设存在 `class 2`。
- 新方向：直接使用无分类地面近似作为基准，不把“没有 class 2”当 warning 或异常。
- 报告现在已包含更完整的项目头部与风险摘要信息
- 当前体积报告已具备正式交付感，且结果/UI/报告/报告视觉都有自动回归
- 用户新决定：体积面板里不再单独暴露 `Base Surface` 结果卡和对应交互；认为这部分过于复杂，应简化 UI。
